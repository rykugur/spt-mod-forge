# Rust
/target/
**/*.rs.bk
*.pdb

# Build artifacts (from nix build)
result
result-*

# Nix / devenv / direnv
.direnv/
.devenv/
devenv.local.nix
devenv.local.yaml

# .envrc is committed on purpose (for direnv + `use devenv` or `use flake`)
# Run `direnv allow` after cloning.

# Project specific
**/.superpowers/
*.log

# Editor / OS
.idea/
.vscode/
*.swp
*~
.DS_Store
Thumbs.db

# Temporary
/tmp/
*.tmp

# pre-commit
.pre-commit-config.yaml

# Git worktrees (isolated dev workspaces per using-git-worktrees skill)
.worktrees/
worktrees/

# App runtime (XDG paths, never CWD, but ignore in case of manual copies)
*.db
state.db
spt-mod-forge.db
# Cache (user may have local .cache copy during dev)
.cache/
